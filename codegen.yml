overwrite: true
schema: "http://127.0.0.1:3000/graphql"
documents:
  - "app/api/*.graphql"
generates:
  ./generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
    config:
      avoidOptionals:
        objet: true
        field: true